// // client/src/environments/environment.prod.js
// export const environment = {
//     production: true, // Indicates this is a production build
//     apiUrl: '/api' //  Backend API URL in production (could be relative to the frontend domain if backend and frontend are served from the same domain/origin in production)
//   };
export const environment = {
  production: true,
  apiUrl: '/api' // Relative URL for production if frontend and backend on same domain
};